---
import { siteConfig } from "../config";
---

<div
  id="hero"
  class="relative isolate overflow-hidden bg-white py-24 md:min-h-screen"
  style={`--accent-color: ${siteConfig.accentColor}`}
>
  <!-- Animated Background -->
  <div class="absolute inset-0 -z-10 overflow-hidden">
    <!-- Animated gradient background -->
    <div
      class="absolute inset-0 animate-gradient"
      style={`background: linear-gradient(45deg, ${siteConfig.accentColor}22, ${siteConfig.accentColor}14, ${siteConfig.accentColor}18, ${siteConfig.accentColor}10)`}
    ></div>
  </div>
    
  <div class="h-full mx-auto px-8 py-8 sm:px-12 sm:py-10 md:px-24 md:pt-24 md:pb-8 flex items-center justify-center">
    <div class="relative w-full max-w-5xl text-center">
      
      <!-- Logo -->
      <div class="mb-8 animate-fade-in">
        <img 
          src={siteConfig.logo}
          alt={siteConfig.logoAlt}
          class="w-32 h-32 sm:w-40 sm:h-40 md:w-48 md:h-48 rounded-full mx-auto border-4 border-gray-200 shadow-lg hover:border-[var(--accent-color)] transition-all duration-300"
        />
      </div>
      
      <h2
        class="text-pretty text-xl sm:text-2xl md:text-4xl font-bold tracking-tight text-gray-700 animate-fade-in animation-delay-200"
      >
        ¡Bienvenidos! 👋
      </h2>
      <h1
        class="mt-6 sm:mt-8 md:mt-10 text-pretty text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold tracking-tight text-gray-800 animate-fade-in animation-delay-400"
      >
        <span style={{ color: siteConfig.accentColor }}
          >{siteConfig.name}</span
        >
      </h1>
      <p
        class="mt-4 sm:mt-6 md:mt-8 text-pretty text-lg sm:text-xl md:text-2xl leading-relaxed font-medium text-gray-600 animate-fade-in animation-delay-600"
      >
        A monthly seminar series showcasing Research Software Engineers<br/>🌎 across the Spanish-speaking world 🌍
      </p>
      
      <!-- Tags -->
      <div class="mt-4 sm:mt-6 md:mt-8 flex flex-wrap gap-2 sm:gap-3 justify-center animate-fade-in animation-delay-800">
        <span class="px-3 py-1.5 rounded-full text-base sm:text-lg font-medium border-2" style={`background-color: ${siteConfig.accentColor}15; color: ${siteConfig.accentColor}; border-color: ${siteConfig.accentColor}30;`}>
          monthly series
        </span>
        <span class="px-3 py-1.5 rounded-full text-base sm:text-lg font-medium border-2" style={`background-color: ${siteConfig.accentColor}15; color: ${siteConfig.accentColor}; border-color: ${siteConfig.accentColor}30;`}>
          online + in-person
        </span>
        <span class="px-3 py-1.5 rounded-full text-base sm:text-lg font-medium border-2" style={`background-color: ${siteConfig.accentColor}15; color: ${siteConfig.accentColor}; border-color: ${siteConfig.accentColor}30;`}>
          se habla español
        </span>
        <span class="px-3 py-1.5 rounded-full text-base sm:text-lg font-medium border-2" style={`background-color: ${siteConfig.accentColor}15; color: ${siteConfig.accentColor}; border-color: ${siteConfig.accentColor}30;`}>
          research software
        </span>
        <span class="px-3 py-1.5 rounded-full text-base sm:text-lg font-medium border-2" style={`background-color: ${siteConfig.accentColor}15; color: ${siteConfig.accentColor}; border-color: ${siteConfig.accentColor}30;`}>
          open to all
        </span>
      </div>
      

      
      <!-- RSE clarification accordion -->
      <div class="mt-12 sm:mt-16 md:mt-20 animate-fade-in animation-delay-1200 max-w-3xl mx-auto">
        <details class="group rounded-2xl shadow-lg border-2 transition-all duration-300 hover:shadow-xl"
                 style={`background-color: ${siteConfig.accentColor}25; border-color: ${siteConfig.accentColor}05`}>
          <summary class="cursor-pointer p-6 sm:p-8 flex items-center justify-between list-none">
            <span class="text-lg sm:text-xl md:text-2xl font-bold text-gray-800">
              <i>Disculpa, ¿qué es un RSE?</i> 🤔
            </span>
            <svg 
              class="w-6 h-6 transition-transform duration-300 group-open:rotate-180" 
              style={`color: ${siteConfig.accentColor}`}
              fill="none" 
              stroke="currentColor" 
              viewBox="0 0 24 24"
            >
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/>
            </svg>
          </summary>
          <div class="px-6 sm:px-8 pb-6 sm:pb-8 border-t pt-6" 
               style={`border-color: ${siteConfig.accentColor}20`}>
            <div class="space-y-4">
              <p class="text-lg sm:text-xl md:text-2xl font-semibold" style={`color: ${siteConfig.accentColor}`}>
                Research Software Engineer = Ingeniero de Software de Investigación
              </p>
              <p class="text-base sm:text-lg md:text-xl text-gray-700">
                Si te gusta programar para la ciencia, o si te interesa cómo se hace software en la investigación, ¡sigue leyendo! 🚀
              </p>
            </div>
          </div>
        </details>
      </div>
      </div>
    </div>

</div>
