---
import { siteConfig } from "../config";

const speaker = siteConfig.nextSpeaker;
---

<section id={siteConfig.sections.nextSpeaker.id} class="p-8 sm:p-12 md:p-16 lg:p-24 bg-gradient-to-br from-gray-50 to-white" style={`--accent-color: ${siteConfig.accentColor}`}>
  <div>
    <div class="mb-12">
      <h2
        class="text-2xl sm:text-3xl md:text-4xl xl:text-5xl font-bold text-gray-900"
      >
        {siteConfig.sections.nextSpeaker.title}
      </h2>
      <div
        class="w-[75px] h-[5px] mt-2 rounded-full"
        style={`background-color: ${siteConfig.accentColor}`}
      >
      </div>
    </div>

    <div class="max-w-5xl mx-auto">
      <!-- Speaker Card -->
      <div class="bg-white rounded-2xl shadow-xl border border-gray-200 overflow-hidden">
        <!-- Speaker Header -->
        <div class="px-6 pt-6 pb-4 sm:px-8 sm:pt-8 sm:pb-5 md:px-10 md:pt-10 md:pb-6" style={`background: linear-gradient(135deg, ${siteConfig.accentColor}15 0%, ${siteConfig.accentColor}05 100%)`}>
          <div class="mb-4">
            <h3 class="text-xl sm:text-2xl md:text-3xl font-bold text-gray-900">
              {speaker.name}
            </h3>
            <p class="text-2xl sm:text-3xl md:text-4xl font-semibold text-gray-700 italic mt-2">
              {speaker.title}
            </p>
          </div>
          
          <p class="text-base sm:text-lg text-gray-700 font-medium">
            {speaker.institution}
          </p>
        </div>

        <!-- Abstract Section -->
        <details class="group p-6 sm:p-8 md:p-10" open>
        <summary class="cursor-pointer text-base sm:text-lg font-bold text-gray-900 flex items-center justify-between hover:text-[var(--accent-color)] transition-colors">
            <span>🦉 Abstract</span>
            <svg class="w-5 h-5 transition-transform group-open:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
            </svg>
          </summary>
          <div class="mt-4 text-sm sm:text-base text-gray-700 leading-relaxed">
            <p set:html={speaker.abstract}></p>
          </div>
        </Details>

        <!-- Bio Section -->
        <details class="group p-6 sm:p-8 md:p-10" open>
          <summary class="cursor-pointer text-base sm:text-lg font-bold text-gray-900 flex items-center justify-between hover:text-[var(--accent-color)] transition-colors">
            <span>🦜 Speaker bio</span>
            <svg class="w-5 h-5 transition-transform group-open:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
            </svg>
          </summary>
          <div class="mt-4 text-sm sm:text-base text-gray-700 leading-relaxed">
            <p set:html={speaker.bio}></p>
          </div>
        </details>
     

        <!-- Session Details -->
        <div class="p-6 sm:p-8 md:p-10 border-b border-gray-200 bg-gray-50">
          <h5 class="text-lg font-bold text-gray-900 mb-4 flex items-center gap-2">
          </h5>
          <div class="space-y-3 text-base sm:text-lg text-gray-700">
            <div class="flex items-start gap-3">
              <span class="font-bold min-w-[100px]">🗣 What?</span>
              <span>¡Tech talks en español!</span>
            </div>
            <div class="flex items-start gap-3">
              <span class="font-bold min-w-[100px]">🕓 When?</span>
              <span>{speaker.date}, {speaker.time}</span>
            </div>
            <div class="flex items-start gap-3">
              <span class="font-bold min-w-[100px]">📍 Where?</span>
              <span set:html={speaker.location}></span>
            </div>
          </div>

          <!-- Action Buttons -->
          <div class="mt-6 flex flex-col sm:flex-row gap-4">
            <a
              href={speaker.calendarLink}
              target="_blank"
              rel="noopener noreferrer"
              class="inline-flex items-center justify-center gap-2 px-6 py-3 rounded-lg font-semibold text-white transition-all duration-300 hover:shadow-lg hover:scale-105"
              style={`background-color: ${siteConfig.accentColor}`}
            >
              <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path stroke="none" d="M0 0h24v24H0z" fill="none"/>
                <path d="M4 7a2 2 0 0 1 2 -2h12a2 2 0 0 1 2 2v12a2 2 0 0 1 -2 2h-12a2 2 0 0 1 -2 -2v-12z" />
                <path d="M16 3v4" />
                <path d="M8 3v4" />
                <path d="M4 11h16" />
              </svg>
              Calendar invite (with Zoom details)
            </a>
            <a
              href={`mailto:${siteConfig.social.email}`}
              class="inline-flex items-center justify-center gap-2 px-6 py-3 bg-gray-200 text-gray-800 rounded-lg font-semibold transition-all duration-300 hover:bg-gray-300 hover:shadow-lg hover:scale-105"
            >
              <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path stroke="none" d="M0 0h24v24H0z" fill="none"/>
                <path d="M3 7a2 2 0 0 1 2 -2h14a2 2 0 0 1 2 2v10a2 2 0 0 1 -2 2h-14a2 2 0 0 1 -2 -2v-10z" />
                <path d="M3 7l9 6l9 -6" />
              </svg>
              RSVP for in-person (limited spots!)
            </a>
          </div>
        </div>

      </div>

      <!-- Additional Info -->
      <div class="mt-8 pt-4 pb-2 sm:pt-6 sm:pb-3 md:pt-8 md:pb-4 text-center text-gray-600">
        <p class="text-lg sm:text-xl md:text-2xl">
          Can't join this time? Subscribe to our 
          <a href={siteConfig.social.email ? `${siteConfig.social.mailingList}` : '#'} class="font-bold text-[var(--accent-color)] hover:opacity-80 transition-opacity">mailing list</a>
          to stay informed!  <span style='font-size: 1.25em;'>📬</span>
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  details summary::-webkit-details-marker {
    display: none;
  }
</style>

